# Template only — do not commit real values
# Apply with: kubectl apply -f k8s/secret.yaml after filling in values
apiVersion: v1
kind: Secret
metadata:
  name: worker-secrets
  namespace: romulus
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
