apiVersion: v1
kind: Service
metadata:
  name: romulus-backend
  namespace: romulus
spec:
  selector:
    app: romulus-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
